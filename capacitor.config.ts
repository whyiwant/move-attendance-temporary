import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'move-attendance-temporary',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
