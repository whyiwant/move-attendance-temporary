import { NamesPipePipe } from './names-pipe.pipe';

describe('NamesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NamesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
